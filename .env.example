# ─── Configuración de entorno — Call Center Analytics ───────────────────────
# Copia este archivo a `.env` y completa los valores. NO commitees el .env real.

# URL base de la API backend (incluye /api)
# Producción (por defecto): https://metricas.macsalud.com/api
# Para probar contra la LAN, descomenta la línea local:
API_URL=https://metricas.macsalud.com/api
# API_URL=http://192.168.11.3/api

# Versión de la API
API_VERSION=v1

# WebSocket (tiempo real), si aplica
WS_URL=wss://metricas.macsalud.com

# Entorno: development | staging | production
APP_ENV=development
